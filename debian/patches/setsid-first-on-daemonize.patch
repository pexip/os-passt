Index: passt/util.c
===================================================================
--- passt.orig/util.c
+++ passt/util.c
@@ -499,8 +499,11 @@ int output_file_open(const char *path, i
  */
 int __daemon(int pidfile_fd, int devnull_fd)
 {
-	pid_t pid = fork();
+	pid_t pid;
 
+	setsid();
+
+	pid = fork();
 	if (pid == -1) {
 		perror("fork");
 		_exit(EXIT_FAILURE);
